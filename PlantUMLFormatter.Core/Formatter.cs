namespace PlantUMLFormatter.Core;

public static class Formatter
{
  public static string Format(string content)
  {
    // This is a placeholder for the actual formatting logic.
    // In a real implementation, you would parse the content,
    // apply formatting rules, and return the formatted string.
    return content;
  }
}
